#!/usr/bin/env python

import sys
sys.path.insert(1, '@pythondir@')

from PRIMEselector.application import PRIMEselector

if __name__ == "__main__":
    app = PRIMEselector(pkgdatadir="@pkgdatadir@", package="@PACKAGE@")
    app.run()
